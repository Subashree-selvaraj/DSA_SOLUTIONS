        dp[rows][cols - 1] = 1;        dp[rows - 1][cols] = 1;        for (int i = rows - 1; i >= 0; i--) {            for (int j = cols - 1; j >= 0; j--) {                                int minHealthFromNext = Math.min(dp[i + 1][j], dp[i][j + 1]);                              dp[i][j] = Math.max(1, minHealthFromNext - dungeon[i][j]);            }        }        return dp[0][0];    }}        }        int cols = dungeon[0].length;         int[][] dp = new int[rows + 1][cols + 1];        for (int[] row : dp) {            Arrays.fill(row, Integer.MAX_VALUE);